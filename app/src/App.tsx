import { BrowserRouter as Router } from "react-router-dom";

import { Routes } from "routes";

const App = (): JSX.Element => {
  return (
    <Router>
      <Routes />
    </Router>
  );
};

export default App;
